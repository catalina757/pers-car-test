angular.module('taxes-app').controller('mainCtrl', mainCtrl);
mainCtrl.$inject = ['$scope', '$loading'];
function mainCtrl($scope, $loading) {

	// $scope.startLoader = () => $loading.start('loading-container');
	// $scope.stopLoader = () => $loading.finish('loading-container');

}